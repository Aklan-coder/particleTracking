# MATCHED GEOMETRY vs LEARNED CLASSIFIER REPORT

**Interpretation rule, preserved exactly as instructed:** the most this evidence can support, if the numbers show it, is: *"The learned classifier retains discriminative capability under spatial representations where explicit geometric fitting becomes unreliable."* This does NOT establish an edge-radar hardware/cost conclusion -- h is a depth-derived simulated spatial cell size here, not a validated physical radar resolution specification.


# MATCHED GEOMETRY vs LEARNED CLASSIFIER
## PRIMARY MATCHED COMPARISON
## TRAINING-OVERLAP AUDIT (Section 18) -- critical interpretation gate
**MOST/MANY matched samples were used in learned-model TRAIN.** **THE MATCHED COMPARISON IS THEREFORE DIAGNOSTIC, NOT AN UNBIASED GENERALIZATION TEST for the learned classifier**, per your instruction #18. This does not invalidate the comparison's value for understanding what information each REPRESENTATION retains at each h, but it must not be presented as unbiased learned-model accuracy.

## INDEPENDENT LEARNED-CLASSIFIER GENERALIZATION (already-validated, reported separately, NOT recomputed)